---
layout: ../layouts/Page.astro
title: QuakeWorld Resources
---

import { resources } from "./../data/resources.js";
import { toItemCollections } from "../util";

export const collections = toItemCollections(resources);

export const published = new Date('2022-02-01'); // available in mdx

<div className="font-bold text-lg">Collection of QuakeWorld related resources.</div>

---

<div className="space-y-6">
  {
    collections.map(c => (
      <div>
        <div className="font-bold text-2xl py-2 pl-4 border-l-4 border-l-purple-400 bg-purple-400/10 flex">{c.category.title} ({c.items.length})</div>
        <div className="py-2 grid grid-cols-4">
          {
            c.items.map(item => (
              <div><a href={item.url}>{item.title}</a></div>
            ))
          }
        </div>
      </div>
    ))
  }
</div>
